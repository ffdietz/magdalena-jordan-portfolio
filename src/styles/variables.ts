import { css } from "styled-components";

const variables = css`

  :root{
    --background-color: #FFF;
    --font-color: #000;
    --active-link: #00F;
    --hover-link: #00F;
    --navbar-height: 8vh;
  }
`;

export default variables